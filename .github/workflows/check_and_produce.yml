        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
          PRIVACY_STATUS: "public"
          CONTENT_LANGUAGE: "en"
          TTS_VOICE: "en-US-AriaNeural"
          AUTO_TRIGGER_TIME: "16:30"      # "video uret" yazilmazsa gunun dongusu bu Istanbul saatinde kendiliginden baslar
          PUBLISH_TIME_ISTANBUL: "20:00"  # onaylanan video YouTube'un kendi zamanlamasiyla bu saatte yayina girer. Hemen yayinlansin istersen "off" yap.
          FALLBACK_HOURS: "20"
          MAX_REDO_ATTEMPTS: "5"
